//
// InvestmentsAuthGetResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** InvestmentsAuthGetResponse defines the response schema for &#x60;/investments/auth/get&#x60; */
public struct InvestmentsAuthGetResponse: Codable, JSONEncodable, Hashable {

    /** The accounts for which data is being retrieved */
    public var accounts: [AccountBase]
    /** The holdings belonging to investment accounts associated with the Item. Details of the securities in the holdings are provided in the `securities` field.  */
    public var holdings: [Holding]
    /** Objects describing the securities held in the accounts associated with the Item.  */
    public var securities: [Security]
    /** Information about the account owners for the accounts associated with the Item.  */
    public var owners: [InvestmentsAuthOwner]
    public var numbers: InvestmentsAuthGetNumbers
    public var item: Item
    /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
    public var requestId: String

    public init(accounts: [AccountBase], holdings: [Holding], securities: [Security], owners: [InvestmentsAuthOwner], numbers: InvestmentsAuthGetNumbers, item: Item, requestId: String) {
        self.accounts = accounts
        self.holdings = holdings
        self.securities = securities
        self.owners = owners
        self.numbers = numbers
        self.item = item
        self.requestId = requestId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accounts
        case holdings
        case securities
        case owners
        case numbers
        case item
        case requestId = "request_id"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(accounts, forKey: .accounts)
        try container.encode(holdings, forKey: .holdings)
        try container.encode(securities, forKey: .securities)
        try container.encode(owners, forKey: .owners)
        try container.encode(numbers, forKey: .numbers)
        try container.encode(item, forKey: .item)
        try container.encode(requestId, forKey: .requestId)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        accounts = try container.decode([AccountBase].self, forKey: .accounts)
        holdings = try container.decode([Holding].self, forKey: .holdings)
        securities = try container.decode([Security].self, forKey: .securities)
        owners = try container.decode([InvestmentsAuthOwner].self, forKey: .owners)
        numbers = try container.decode(InvestmentsAuthGetNumbers.self, forKey: .numbers)
        item = try container.decode(Item.self, forKey: .item)
        requestId = try container.decode(String.self, forKey: .requestId)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("accounts")
        nonAdditionalPropertyKeys.insert("holdings")
        nonAdditionalPropertyKeys.insert("securities")
        nonAdditionalPropertyKeys.insert("owners")
        nonAdditionalPropertyKeys.insert("numbers")
        nonAdditionalPropertyKeys.insert("item")
        nonAdditionalPropertyKeys.insert("request_id")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

