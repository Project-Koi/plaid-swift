//
// CreditRelayCreateRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** CreditRelayCreateRequest defines the request schema for &#x60;/credit/relay/create&#x60; */
public struct CreditRelayCreateRequest: Codable, JSONEncodable, Hashable {

    /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
    public var clientId: String?
    /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
    public var secret: String?
    /** List of report token strings, with at most one token of each report type. Currently only Asset Report token is supported. */
    public var reportTokens: [String]
    /** The `secondary_client_id` is the client id of the third party with whom you would like to share the relay token. */
    public var secondaryClientId: String
    /** URL to which Plaid will send webhooks when the Secondary Client successfully retrieves an Asset Report by calling `/credit/relay/get`. */
    public var webhook: String?

    public init(clientId: String? = nil, secret: String? = nil, reportTokens: [String], secondaryClientId: String, webhook: String? = nil) {
        self.clientId = clientId
        self.secret = secret
        self.reportTokens = reportTokens
        self.secondaryClientId = secondaryClientId
        self.webhook = webhook
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientId = "client_id"
        case secret
        case reportTokens = "report_tokens"
        case secondaryClientId = "secondary_client_id"
        case webhook
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(secret, forKey: .secret)
        try container.encode(reportTokens, forKey: .reportTokens)
        try container.encode(secondaryClientId, forKey: .secondaryClientId)
        try container.encodeIfPresent(webhook, forKey: .webhook)
    }
}

