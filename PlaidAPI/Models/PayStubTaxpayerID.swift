//
// PayStubTaxpayerID.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Taxpayer ID of the individual receiving the paystub. */
public struct PayStubTaxpayerID: Codable, JSONEncodable, Hashable {

    /** Type of ID, e.g. 'SSN'. */
    public var idType: String?
    /** ID mask; i.e. last 4 digits of the taxpayer ID. */
    public var idMask: String?

    public init(idType: String?, idMask: String?) {
        self.idType = idType
        self.idMask = idMask
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case idType = "id_type"
        case idMask = "id_mask"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(idType, forKey: .idType)
        try container.encode(idMask, forKey: .idMask)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        idType = try container.decodeIfPresent(String.self, forKey: .idType)
        idMask = try container.decodeIfPresent(String.self, forKey: .idMask)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("id_type")
        nonAdditionalPropertyKeys.insert("id_mask")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

