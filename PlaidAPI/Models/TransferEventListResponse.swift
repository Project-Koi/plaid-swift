//
// TransferEventListResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Defines the response schema for &#x60;/transfer/event/list&#x60; */
public struct TransferEventListResponse: Codable, JSONEncodable, Hashable {

    public var transferEvents: [TransferEvent]
    /** Whether there are more events to be pulled from the endpoint that have not already been returned */
    public var hasMore: Bool
    /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
    public var requestId: String

    public init(transferEvents: [TransferEvent], hasMore: Bool, requestId: String) {
        self.transferEvents = transferEvents
        self.hasMore = hasMore
        self.requestId = requestId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case transferEvents = "transfer_events"
        case hasMore = "has_more"
        case requestId = "request_id"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(transferEvents, forKey: .transferEvents)
        try container.encode(hasMore, forKey: .hasMore)
        try container.encode(requestId, forKey: .requestId)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        transferEvents = try container.decode([TransferEvent].self, forKey: .transferEvents)
        hasMore = try container.decode(Bool.self, forKey: .hasMore)
        requestId = try container.decode(String.self, forKey: .requestId)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("transfer_events")
        nonAdditionalPropertyKeys.insert("has_more")
        nonAdditionalPropertyKeys.insert("request_id")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

