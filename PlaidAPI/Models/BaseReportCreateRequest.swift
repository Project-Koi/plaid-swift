//
// BaseReportCreateRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** BaseReportCreateRequest defines the request schema for &#x60;/cra/base_report/create&#x60; */
public struct BaseReportCreateRequest: Codable, JSONEncodable, Hashable {

    static let daysRequestedRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: 731, exclusiveMaximum: false, multipleOf: nil)
    /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
    public var clientId: String?
    /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
    public var secret: String?
    /** The user token associated with the User data is being requested for. */
    public var userToken: String
    /** The duration of transaction history you requested */
    public var daysRequested: Int
    /** URL to which Plaid will send Assets webhooks, for example when the requested Asset Report is ready. */
    public var webhook: String?
    public var consumerReportPermissiblePurpose: ConsumerReportPermissiblePurpose

    public init(clientId: String? = nil, secret: String? = nil, userToken: String, daysRequested: Int, webhook: String?, consumerReportPermissiblePurpose: ConsumerReportPermissiblePurpose) {
        self.clientId = clientId
        self.secret = secret
        self.userToken = userToken
        self.daysRequested = daysRequested
        self.webhook = webhook
        self.consumerReportPermissiblePurpose = consumerReportPermissiblePurpose
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientId = "client_id"
        case secret
        case userToken = "user_token"
        case daysRequested = "days_requested"
        case webhook
        case consumerReportPermissiblePurpose = "consumer_report_permissible_purpose"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(secret, forKey: .secret)
        try container.encode(userToken, forKey: .userToken)
        try container.encode(daysRequested, forKey: .daysRequested)
        try container.encode(webhook, forKey: .webhook)
        try container.encode(consumerReportPermissiblePurpose, forKey: .consumerReportPermissiblePurpose)
    }
}

