//
// LinkTokenCreateResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** LinkTokenCreateResponse defines the response schema for &#x60;/link/token/create&#x60; */
public struct LinkTokenCreateResponse: Codable, JSONEncodable, Hashable {

    /** A `link_token`, which can be supplied to Link in order to initialize it and receive a `public_token`, which can be exchanged for an `access_token`. */
    public var linkToken: String
    /** The expiration date for the `link_token`, in [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format. A `link_token` created to generate a `public_token` that will be exchanged for a new `access_token` expires after 4 hours. A `link_token` created for an existing Item (such as when updating an existing `access_token` by launching Link in update mode) expires after 30 minutes. */
    public var expiration: Date
    /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
    public var requestId: String
    /** A URL of a Plaid-hosted Link flow that will use the Link token returned by this request. Only present if the client is enabled for Hosted Link. */
    public var hostedLinkUrl: String?

    public init(linkToken: String, expiration: Date, requestId: String, hostedLinkUrl: String? = nil) {
        self.linkToken = linkToken
        self.expiration = expiration
        self.requestId = requestId
        self.hostedLinkUrl = hostedLinkUrl
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case linkToken = "link_token"
        case expiration
        case requestId = "request_id"
        case hostedLinkUrl = "hosted_link_url"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(linkToken, forKey: .linkToken)
        try container.encode(expiration, forKey: .expiration)
        try container.encode(requestId, forKey: .requestId)
        try container.encodeIfPresent(hostedLinkUrl, forKey: .hostedLinkUrl)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        linkToken = try container.decode(String.self, forKey: .linkToken)
        expiration = try container.decode(Date.self, forKey: .expiration)
        requestId = try container.decode(String.self, forKey: .requestId)
        hostedLinkUrl = try container.decodeIfPresent(String.self, forKey: .hostedLinkUrl)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("link_token")
        nonAdditionalPropertyKeys.insert("expiration")
        nonAdditionalPropertyKeys.insert("request_id")
        nonAdditionalPropertyKeys.insert("hosted_link_url")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

