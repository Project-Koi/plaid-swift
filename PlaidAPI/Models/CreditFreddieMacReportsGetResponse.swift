//
// CreditFreddieMacReportsGetResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** CreditFreddieMacReportsGetResponse defines the response schema for &#x60;/credit/freddie_mac/reports/get&#x60; */
public struct CreditFreddieMacReportsGetResponse: Codable, JSONEncodable, Hashable {

    public var DEAL: CreditFreddieMacVerificationOfAssetsDeal
    /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
    public var requestId: String
    /** The Verification Of Assets (VOA) schema version. */
    public var schemaVersion: Double

    public init(DEAL: CreditFreddieMacVerificationOfAssetsDeal, requestId: String, schemaVersion: Double) {
        self.DEAL = DEAL
        self.requestId = requestId
        self.schemaVersion = schemaVersion
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case DEAL
        case requestId = "request_id"
        case schemaVersion = "SchemaVersion"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(DEAL, forKey: .DEAL)
        try container.encode(requestId, forKey: .requestId)
        try container.encode(schemaVersion, forKey: .schemaVersion)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        DEAL = try container.decode(CreditFreddieMacVerificationOfAssetsDeal.self, forKey: .DEAL)
        requestId = try container.decode(String.self, forKey: .requestId)
        schemaVersion = try container.decode(Double.self, forKey: .schemaVersion)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("DEAL")
        nonAdditionalPropertyKeys.insert("request_id")
        nonAdditionalPropertyKeys.insert("SchemaVersion")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

