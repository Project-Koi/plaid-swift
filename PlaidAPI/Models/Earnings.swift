//
// Earnings.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** An object representing both a breakdown of earnings on a paystub and the total earnings. */
public struct Earnings: Codable, JSONEncodable, Hashable {

    @available(*, deprecated, message: "This property is deprecated.")
    public var subtotals: [EarningsTotal]?
    @available(*, deprecated, message: "This property is deprecated.")
    public var totals: [EarningsTotal]?
    public var breakdown: [EarningsBreakdown]?
    public var total: EarningsTotal?

    public init(subtotals: [EarningsTotal]? = nil, totals: [EarningsTotal]? = nil, breakdown: [EarningsBreakdown]? = nil, total: EarningsTotal? = nil) {
        self.subtotals = subtotals
        self.totals = totals
        self.breakdown = breakdown
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case subtotals
        case totals
        case breakdown
        case total
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(subtotals, forKey: .subtotals)
        try container.encodeIfPresent(totals, forKey: .totals)
        try container.encodeIfPresent(breakdown, forKey: .breakdown)
        try container.encodeIfPresent(total, forKey: .total)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        subtotals = try container.decodeIfPresent([EarningsTotal].self, forKey: .subtotals)
        totals = try container.decodeIfPresent([EarningsTotal].self, forKey: .totals)
        breakdown = try container.decodeIfPresent([EarningsBreakdown].self, forKey: .breakdown)
        total = try container.decodeIfPresent(EarningsTotal.self, forKey: .total)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("subtotals")
        nonAdditionalPropertyKeys.insert("totals")
        nonAdditionalPropertyKeys.insert("breakdown")
        nonAdditionalPropertyKeys.insert("total")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

