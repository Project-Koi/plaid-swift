//
// LoanIdentifiers.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Collection of current and previous identifiers for this loan. */
public struct LoanIdentifiers: Codable, JSONEncodable, Hashable {

    public var LOAN_IDENTIFIER: LoanIdentifier

    public init(LOAN_IDENTIFIER: LoanIdentifier) {
        self.LOAN_IDENTIFIER = LOAN_IDENTIFIER
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case LOAN_IDENTIFIER
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(LOAN_IDENTIFIER, forKey: .LOAN_IDENTIFIER)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        LOAN_IDENTIFIER = try container.decode(LoanIdentifier.self, forKey: .LOAN_IDENTIFIER)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("LOAN_IDENTIFIER")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

