//
// CreditEmploymentVerification.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The object containing proof of employment data for an individual. */
public struct CreditEmploymentVerification: Codable, JSONEncodable, Hashable {

    /** ID of the payroll provider account. */
    public var accountId: String?
    /** Current employment status. */
    public var status: String?
    /** Start of employment in ISO 8601 format (YYYY-MM-DD). */
    public var startDate: Date?
    /** End of employment, if applicable. Provided in ISO 8601 format (YYY-MM-DD). */
    public var endDate: Date?
    public var employer: CreditEmployerVerification
    /** Current title of employee. */
    public var title: String?
    public var platformIds: CreditPlatformIds
    /** The type of employment for the individual. `\"FULL_TIME\"`: A full-time employee. `\"PART_TIME\"`: A part-time employee. `\"CONTRACTOR\"`: An employee typically hired externally through a contracting group. `\"TEMPORARY\"`: A temporary employee. `\"OTHER\"`: The employee type is not one of the above defined types. */
    public var employeeType: String?
    /** The date of the employee's most recent paystub in ISO 8601 format (YYYY-MM-DD). */
    public var lastPaystubDate: Date?

    public init(accountId: String?, status: String?, startDate: Date?, endDate: Date?, employer: CreditEmployerVerification, title: String?, platformIds: CreditPlatformIds, employeeType: String?, lastPaystubDate: Date?) {
        self.accountId = accountId
        self.status = status
        self.startDate = startDate
        self.endDate = endDate
        self.employer = employer
        self.title = title
        self.platformIds = platformIds
        self.employeeType = employeeType
        self.lastPaystubDate = lastPaystubDate
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accountId = "account_id"
        case status
        case startDate = "start_date"
        case endDate = "end_date"
        case employer
        case title
        case platformIds = "platform_ids"
        case employeeType = "employee_type"
        case lastPaystubDate = "last_paystub_date"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(accountId, forKey: .accountId)
        try container.encode(status, forKey: .status)
        try container.encode(startDate, forKey: .startDate)
        try container.encode(endDate, forKey: .endDate)
        try container.encode(employer, forKey: .employer)
        try container.encode(title, forKey: .title)
        try container.encode(platformIds, forKey: .platformIds)
        try container.encode(employeeType, forKey: .employeeType)
        try container.encode(lastPaystubDate, forKey: .lastPaystubDate)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        accountId = try container.decodeIfPresent(String.self, forKey: .accountId)
        status = try container.decodeIfPresent(String.self, forKey: .status)
        startDate = try container.decodeIfPresent(Date.self, forKey: .startDate)
        endDate = try container.decodeIfPresent(Date.self, forKey: .endDate)
        employer = try container.decode(CreditEmployerVerification.self, forKey: .employer)
        title = try container.decodeIfPresent(String.self, forKey: .title)
        platformIds = try container.decode(CreditPlatformIds.self, forKey: .platformIds)
        employeeType = try container.decodeIfPresent(String.self, forKey: .employeeType)
        lastPaystubDate = try container.decodeIfPresent(Date.self, forKey: .lastPaystubDate)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("account_id")
        nonAdditionalPropertyKeys.insert("status")
        nonAdditionalPropertyKeys.insert("start_date")
        nonAdditionalPropertyKeys.insert("end_date")
        nonAdditionalPropertyKeys.insert("employer")
        nonAdditionalPropertyKeys.insert("title")
        nonAdditionalPropertyKeys.insert("platform_ids")
        nonAdditionalPropertyKeys.insert("employee_type")
        nonAdditionalPropertyKeys.insert("last_paystub_date")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

