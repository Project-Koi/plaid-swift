//
// CreditSessionBankEmploymentResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The details of a bank employment verification in Link. */
public struct CreditSessionBankEmploymentResult: Codable, JSONEncodable, Hashable {

    public var status: CreditSessionBankEmploymentStatus?
    /** The Plaid Item ID. The `item_id` is always unique; linking the same account at the same institution twice will result in two Items with different `item_id` values. Like all Plaid identifiers, the `item_id` is case-sensitive. */
    public var itemId: String?
    /** The Plaid Institution ID associated with the Item. */
    public var institutionId: String?

    public init(status: CreditSessionBankEmploymentStatus? = nil, itemId: String? = nil, institutionId: String? = nil) {
        self.status = status
        self.itemId = itemId
        self.institutionId = institutionId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case status
        case itemId = "item_id"
        case institutionId = "institution_id"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(itemId, forKey: .itemId)
        try container.encodeIfPresent(institutionId, forKey: .institutionId)
    }
}

