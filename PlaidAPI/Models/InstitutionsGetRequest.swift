//
// InstitutionsGetRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** InstitutionsGetRequest defines the request schema for &#x60;/institutions/get&#x60; */
public struct InstitutionsGetRequest: Codable, JSONEncodable, Hashable {

    static let countRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 500, exclusiveMaximum: false, multipleOf: nil)
    static let offsetRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
    public var clientId: String?
    /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
    public var secret: String?
    /** The total number of Institutions to return. */
    public var count: Int
    /** The number of Institutions to skip. */
    public var offset: Int
    /** Specify which country or countries to include institutions from, using the ISO-3166-1 alpha-2 country code standard.  In API versions 2019-05-29 and earlier, the `country_codes` parameter is an optional parameter within the `options` object and will default to `[US]` if it is not supplied.  */
    public var countryCodes: [CountryCode]
    public var options: InstitutionsGetRequestOptions?

    public init(clientId: String? = nil, secret: String? = nil, count: Int, offset: Int, countryCodes: [CountryCode], options: InstitutionsGetRequestOptions? = nil) {
        self.clientId = clientId
        self.secret = secret
        self.count = count
        self.offset = offset
        self.countryCodes = countryCodes
        self.options = options
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientId = "client_id"
        case secret
        case count
        case offset
        case countryCodes = "country_codes"
        case options
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(secret, forKey: .secret)
        try container.encode(count, forKey: .count)
        try container.encode(offset, forKey: .offset)
        try container.encode(countryCodes, forKey: .countryCodes)
        try container.encodeIfPresent(options, forKey: .options)
    }
}

