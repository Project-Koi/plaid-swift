//
// IdentityVerificationAutofillUserData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** User information that was autofilled. All this information should be confirmed by the user before using. */
public struct IdentityVerificationAutofillUserData: Codable, JSONEncodable, Hashable {

    public var name: IdentityVerificationResponseUserName?
    public var address: IdentityVerificationAutofillAddress?
    public var idNumber: UserIDNumber?

    public init(name: IdentityVerificationResponseUserName?, address: IdentityVerificationAutofillAddress?, idNumber: UserIDNumber?) {
        self.name = name
        self.address = address
        self.idNumber = idNumber
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case address
        case idNumber = "id_number"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(address, forKey: .address)
        try container.encode(idNumber, forKey: .idNumber)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        name = try container.decodeIfPresent(IdentityVerificationResponseUserName.self, forKey: .name)
        address = try container.decodeIfPresent(IdentityVerificationAutofillAddress.self, forKey: .address)
        idNumber = try container.decodeIfPresent(UserIDNumber.self, forKey: .idNumber)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("name")
        nonAdditionalPropertyKeys.insert("address")
        nonAdditionalPropertyKeys.insert("id_number")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

