//
// Credit1099Payer.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** An object representing a payer used by 1099-MISC tax documents. */
public struct Credit1099Payer: Codable, JSONEncodable, Hashable {

    public var address: CreditPayStubAddress?
    /** Name of payer. */
    public var name: String?
    /** Tax identification number of payer. */
    public var tin: String?
    /** Telephone number of payer. */
    public var telephoneNumber: String?

    public init(address: CreditPayStubAddress? = nil, name: String? = nil, tin: String? = nil, telephoneNumber: String? = nil) {
        self.address = address
        self.name = name
        self.tin = tin
        self.telephoneNumber = telephoneNumber
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case address
        case name
        case tin
        case telephoneNumber = "telephone_number"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(address, forKey: .address)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(tin, forKey: .tin)
        try container.encodeIfPresent(telephoneNumber, forKey: .telephoneNumber)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        address = try container.decodeIfPresent(CreditPayStubAddress.self, forKey: .address)
        name = try container.decodeIfPresent(String.self, forKey: .name)
        tin = try container.decodeIfPresent(String.self, forKey: .tin)
        telephoneNumber = try container.decodeIfPresent(String.self, forKey: .telephoneNumber)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("address")
        nonAdditionalPropertyKeys.insert("name")
        nonAdditionalPropertyKeys.insert("tin")
        nonAdditionalPropertyKeys.insert("telephone_number")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

