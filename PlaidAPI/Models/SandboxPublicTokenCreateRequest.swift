//
// SandboxPublicTokenCreateRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** SandboxPublicTokenCreateRequest defines the request schema for &#x60;/sandbox/public_token/create&#x60; */
public struct SandboxPublicTokenCreateRequest: Codable, JSONEncodable, Hashable {

    /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
    public var clientId: String?
    /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
    public var secret: String?
    /** The ID of the institution the Item will be associated with */
    public var institutionId: String
    /** The products to initially pull for the Item. May be any products that the specified `institution_id`  supports. This array may not be empty. */
    public var initialProducts: [Products]
    public var options: SandboxPublicTokenCreateRequestOptions?
    /** The user token associated with the User data is being requested for. */
    public var userToken: String?

    public init(clientId: String? = nil, secret: String? = nil, institutionId: String, initialProducts: [Products], options: SandboxPublicTokenCreateRequestOptions? = nil, userToken: String? = nil) {
        self.clientId = clientId
        self.secret = secret
        self.institutionId = institutionId
        self.initialProducts = initialProducts
        self.options = options
        self.userToken = userToken
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientId = "client_id"
        case secret
        case institutionId = "institution_id"
        case initialProducts = "initial_products"
        case options
        case userToken = "user_token"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(secret, forKey: .secret)
        try container.encode(institutionId, forKey: .institutionId)
        try container.encode(initialProducts, forKey: .initialProducts)
        try container.encodeIfPresent(options, forKey: .options)
        try container.encodeIfPresent(userToken, forKey: .userToken)
    }
}

