//
// AuthMetadata.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Metadata that captures information about the Auth features of an institution. */
public struct AuthMetadata: Codable, JSONEncodable, Hashable {

    public var supportedMethods: AuthSupportedMethods?

    public init(supportedMethods: AuthSupportedMethods?) {
        self.supportedMethods = supportedMethods
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case supportedMethods = "supported_methods"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(supportedMethods, forKey: .supportedMethods)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        supportedMethods = try container.decodeIfPresent(AuthSupportedMethods.self, forKey: .supportedMethods)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("supported_methods")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

