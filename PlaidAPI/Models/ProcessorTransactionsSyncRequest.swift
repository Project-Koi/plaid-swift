//
// ProcessorTransactionsSyncRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** ProcessorTransactionsSyncRequest defines the request schema for &#x60;/processor/transactions/sync&#x60; */
public struct ProcessorTransactionsSyncRequest: Codable, JSONEncodable, Hashable {

    static let countRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 500, exclusiveMaximum: false, multipleOf: nil)
    /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
    public var clientId: String?
    /** The processor token obtained from the Plaid integration partner. Processor tokens are in the format: `processor-<environment>-<identifier>` */
    public var processorToken: String
    /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
    public var secret: String?
    /** The cursor value represents the last update requested. Providing it will cause the response to only return changes after this update. If omitted, the entire history of updates will be returned, starting with the first-added transactions on the item. Note: The upper-bound length of this cursor is 256 characters of base64. */
    public var cursor: String?
    /** The number of transaction updates to fetch. */
    public var count: Int? = 100
    public var options: TransactionsSyncRequestOptions?

    public init(clientId: String? = nil, processorToken: String, secret: String? = nil, cursor: String? = nil, count: Int? = 100, options: TransactionsSyncRequestOptions? = nil) {
        self.clientId = clientId
        self.processorToken = processorToken
        self.secret = secret
        self.cursor = cursor
        self.count = count
        self.options = options
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientId = "client_id"
        case processorToken = "processor_token"
        case secret
        case cursor
        case count
        case options
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encode(processorToken, forKey: .processorToken)
        try container.encodeIfPresent(secret, forKey: .secret)
        try container.encodeIfPresent(cursor, forKey: .cursor)
        try container.encodeIfPresent(count, forKey: .count)
        try container.encodeIfPresent(options, forKey: .options)
    }
}

