//
// SelfieAnalysis.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** High level descriptions of how the associated selfie was processed. If a selfie fails verification, the details in the &#x60;analysis&#x60; object should help clarify why the selfie was rejected. */
public struct SelfieAnalysis: Codable, JSONEncodable, Hashable {

    public var documentComparison: SelfieAnalysisDocumentComparison

    public init(documentComparison: SelfieAnalysisDocumentComparison) {
        self.documentComparison = documentComparison
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case documentComparison = "document_comparison"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(documentComparison, forKey: .documentComparison)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        documentComparison = try container.decode(SelfieAnalysisDocumentComparison.self, forKey: .documentComparison)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("document_comparison")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

