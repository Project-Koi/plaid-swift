//
// CreditPayStubEarnings.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** An object representing both a breakdown of earnings on a pay stub and the total earnings. */
public struct CreditPayStubEarnings: Codable, JSONEncodable, Hashable {

    public var breakdown: [PayStubEarningsBreakdown]
    public var total: PayStubEarningsTotal

    public init(breakdown: [PayStubEarningsBreakdown], total: PayStubEarningsTotal) {
        self.breakdown = breakdown
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case breakdown
        case total
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(breakdown, forKey: .breakdown)
        try container.encode(total, forKey: .total)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        breakdown = try container.decode([PayStubEarningsBreakdown].self, forKey: .breakdown)
        total = try container.decode(PayStubEarningsTotal.self, forKey: .total)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("breakdown")
        nonAdditionalPropertyKeys.insert("total")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

