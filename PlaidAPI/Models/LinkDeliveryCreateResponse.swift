//
// LinkDeliveryCreateResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** LinkDeliveryCreateResponse defines the response schema for &#x60;/link_delivery/create&#x60; */
public struct LinkDeliveryCreateResponse: Codable, JSONEncodable, Hashable {

    /** The URL to the Hosted Link session, which will be delivered by the specified delivery method. */
    public var linkDeliveryUrl: String
    /** The ID for the Hosted Link session. Same as the `link_token` string excluding the \"link-{env}-\" prefix. */
    public var linkDeliverySessionId: String
    /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
    public var requestId: String

    public init(linkDeliveryUrl: String, linkDeliverySessionId: String, requestId: String) {
        self.linkDeliveryUrl = linkDeliveryUrl
        self.linkDeliverySessionId = linkDeliverySessionId
        self.requestId = requestId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case linkDeliveryUrl = "link_delivery_url"
        case linkDeliverySessionId = "link_delivery_session_id"
        case requestId = "request_id"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(linkDeliveryUrl, forKey: .linkDeliveryUrl)
        try container.encode(linkDeliverySessionId, forKey: .linkDeliverySessionId)
        try container.encode(requestId, forKey: .requestId)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        linkDeliveryUrl = try container.decode(String.self, forKey: .linkDeliveryUrl)
        linkDeliverySessionId = try container.decode(String.self, forKey: .linkDeliverySessionId)
        requestId = try container.decode(String.self, forKey: .requestId)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("link_delivery_url")
        nonAdditionalPropertyKeys.insert("link_delivery_session_id")
        nonAdditionalPropertyKeys.insert("request_id")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

