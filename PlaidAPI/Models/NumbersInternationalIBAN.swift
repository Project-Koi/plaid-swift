//
// NumbersInternationalIBAN.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Account numbers using the International Bank Account Number and BIC/SWIFT code format. */
public struct NumbersInternationalIBAN: Codable, JSONEncodable, Hashable {

    static let ibanRule = StringRule(minLength: 15, maxLength: 34, pattern: nil)
    static let bicRule = StringRule(minLength: 8, maxLength: 11, pattern: nil)
    /** International Bank Account Number (IBAN). */
    public var iban: String
    /** The Business Identifier Code, also known as SWIFT code, for this bank account. */
    public var bic: String

    public init(iban: String, bic: String) {
        self.iban = iban
        self.bic = bic
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case iban
        case bic
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(iban, forKey: .iban)
        try container.encode(bic, forKey: .bic)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        iban = try container.decode(String.self, forKey: .iban)
        bic = try container.decode(String.self, forKey: .bic)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("iban")
        nonAdditionalPropertyKeys.insert("bic")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

