//
// CreditSessionItemAddResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The details of an Item add in Link. */
public struct CreditSessionItemAddResult: Codable, JSONEncodable, Hashable {

    /** Returned once a user has successfully linked their Item. */
    public var publicToken: String?
    /** The Plaid Item ID. The `item_id` is always unique; linking the same account at the same institution twice will result in two Items with different `item_id` values. Like all Plaid identifiers, the `item_id` is case-sensitive. */
    public var itemId: String?
    /** The Plaid Institution ID associated with the Item. */
    public var institutionId: String?

    public init(publicToken: String? = nil, itemId: String? = nil, institutionId: String? = nil) {
        self.publicToken = publicToken
        self.itemId = itemId
        self.institutionId = institutionId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case publicToken = "public_token"
        case itemId = "item_id"
        case institutionId = "institution_id"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(publicToken, forKey: .publicToken)
        try container.encodeIfPresent(itemId, forKey: .itemId)
        try container.encodeIfPresent(institutionId, forKey: .institutionId)
    }
}

