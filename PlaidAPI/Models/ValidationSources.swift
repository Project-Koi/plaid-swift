//
// ValidationSources.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Documentation not found in the MISMO model viewer and not provided by Freddie Mac. */
public struct ValidationSources: Codable, JSONEncodable, Hashable {

    /** Documentation not found in the MISMO model viewer and not provided by Freddie Mac. */
    public var VALIDATION_SOURCE: [ValidationSource]

    public init(VALIDATION_SOURCE: [ValidationSource]) {
        self.VALIDATION_SOURCE = VALIDATION_SOURCE
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case VALIDATION_SOURCE
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(VALIDATION_SOURCE, forKey: .VALIDATION_SOURCE)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        VALIDATION_SOURCE = try container.decode([ValidationSource].self, forKey: .VALIDATION_SOURCE)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("VALIDATION_SOURCE")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

