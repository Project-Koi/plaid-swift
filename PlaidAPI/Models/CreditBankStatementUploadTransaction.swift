//
// CreditBankStatementUploadTransaction.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** An object containing data about a transaction appearing on a user-uploaded bank statement. */
public struct CreditBankStatementUploadTransaction: Codable, JSONEncodable, Hashable {

    /** The value of the transaction. A negative amount indicates that money moved into the account (such as a paycheck being deposited). */
    public var amount: Double?
    /** The date of when the transaction was made, in ISO 8601 format (YYYY-MM-DD). */
    public var date: Date?
    /** The raw description of the transaction as it appears on the bank statement. */
    public var originalDescription: String?
    /** The unique id of the bank account that this transaction occurs in */
    public var accountId: String?

    public init(amount: Double?, date: Date?, originalDescription: String?, accountId: String?) {
        self.amount = amount
        self.date = date
        self.originalDescription = originalDescription
        self.accountId = accountId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case amount
        case date
        case originalDescription = "original_description"
        case accountId = "account_id"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(amount, forKey: .amount)
        try container.encode(date, forKey: .date)
        try container.encode(originalDescription, forKey: .originalDescription)
        try container.encode(accountId, forKey: .accountId)
    }
}

