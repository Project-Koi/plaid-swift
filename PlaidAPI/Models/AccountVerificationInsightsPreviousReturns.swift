//
// AccountVerificationInsightsPreviousReturns.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Information about known ACH returns for the account and routing number. */
public struct AccountVerificationInsightsPreviousReturns: Codable, JSONEncodable, Hashable {

    /** Indicates whether Plaid's data sources include a known administrative ACH return for account and routing number. */
    public var hasPreviousAdministrativeReturn: Bool

    public init(hasPreviousAdministrativeReturn: Bool) {
        self.hasPreviousAdministrativeReturn = hasPreviousAdministrativeReturn
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case hasPreviousAdministrativeReturn = "has_previous_administrative_return"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(hasPreviousAdministrativeReturn, forKey: .hasPreviousAdministrativeReturn)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        hasPreviousAdministrativeReturn = try container.decode(Bool.self, forKey: .hasPreviousAdministrativeReturn)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("has_previous_administrative_return")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

