//
// TransferOriginatorAddress.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The originator&#39;s address. */
public struct TransferOriginatorAddress: Codable, JSONEncodable, Hashable {

    /** The full city name. */
    public var city: String
    /** The full street address. */
    public var street: String
    /** The two-letter code for the state or province (e.g., \"CA\"). */
    public var region: String
    /** The postal code (e.g., \"94103\"). */
    public var postalCode: String
    /** ISO-3166-1 alpha-2 country code standard. */
    public var countryCode: String

    public init(city: String, street: String, region: String, postalCode: String, countryCode: String) {
        self.city = city
        self.street = street
        self.region = region
        self.postalCode = postalCode
        self.countryCode = countryCode
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case city
        case street
        case region
        case postalCode = "postal_code"
        case countryCode = "country_code"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(city, forKey: .city)
        try container.encode(street, forKey: .street)
        try container.encode(region, forKey: .region)
        try container.encode(postalCode, forKey: .postalCode)
        try container.encode(countryCode, forKey: .countryCode)
    }
}

