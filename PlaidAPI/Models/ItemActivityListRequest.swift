//
// ItemActivityListRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Request to list a historical log of user consent events. */
public struct ItemActivityListRequest: Codable, JSONEncodable, Hashable {

    static let countRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 50, exclusiveMaximum: false, multipleOf: nil)
    /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
    public var clientId: String?
    /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
    public var secret: String?
    /** The access token associated with the Item data is being requested for. */
    public var accessToken: String?
    /** Cursor used for pagination. */
    public var cursor: String?
    public var count: Int? = 50

    public init(clientId: String? = nil, secret: String? = nil, accessToken: String? = nil, cursor: String? = nil, count: Int? = 50) {
        self.clientId = clientId
        self.secret = secret
        self.accessToken = accessToken
        self.cursor = cursor
        self.count = count
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientId = "client_id"
        case secret
        case accessToken = "access_token"
        case cursor
        case count
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(secret, forKey: .secret)
        try container.encodeIfPresent(accessToken, forKey: .accessToken)
        try container.encodeIfPresent(cursor, forKey: .cursor)
        try container.encodeIfPresent(count, forKey: .count)
    }
}

