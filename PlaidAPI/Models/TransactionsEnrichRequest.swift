//
// TransactionsEnrichRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** TransactionsEnrichRequest defines the request schema for &#x60;/transactions/enrich&#x60;. */
public struct TransactionsEnrichRequest: Codable, JSONEncodable, Hashable {

    /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
    public var clientId: String?
    /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
    public var secret: String?
    /** The account type for the requested transactions (either `depository` or `credit`). */
    public var accountType: String
    /** An array of transaction objects to be enriched by Plaid. Maximum of 100 transactions per request. */
    public var transactions: [ClientProvidedTransaction]
    public var options: TransactionsEnrichRequestOptions?

    public init(clientId: String? = nil, secret: String? = nil, accountType: String, transactions: [ClientProvidedTransaction], options: TransactionsEnrichRequestOptions? = nil) {
        self.clientId = clientId
        self.secret = secret
        self.accountType = accountType
        self.transactions = transactions
        self.options = options
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientId = "client_id"
        case secret
        case accountType = "account_type"
        case transactions
        case options
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(secret, forKey: .secret)
        try container.encode(accountType, forKey: .accountType)
        try container.encode(transactions, forKey: .transactions)
        try container.encodeIfPresent(options, forKey: .options)
    }
}

