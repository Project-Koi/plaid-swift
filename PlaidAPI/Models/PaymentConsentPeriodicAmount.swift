//
// PaymentConsentPeriodicAmount.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Defines consent payments limitations per period. */
public struct PaymentConsentPeriodicAmount: Codable, JSONEncodable, Hashable {

    public var amount: PaymentConsentPeriodicAmountAmount
    public var interval: PaymentConsentPeriodicInterval
    public var alignment: PaymentConsentPeriodicAlignment

    public init(amount: PaymentConsentPeriodicAmountAmount, interval: PaymentConsentPeriodicInterval, alignment: PaymentConsentPeriodicAlignment) {
        self.amount = amount
        self.interval = interval
        self.alignment = alignment
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case amount
        case interval
        case alignment
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(amount, forKey: .amount)
        try container.encode(interval, forKey: .interval)
        try container.encode(alignment, forKey: .alignment)
    }
}

