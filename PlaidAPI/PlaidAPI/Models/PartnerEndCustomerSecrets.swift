//
// PartnerEndCustomerSecrets.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The secrets for the newly created end customer in non-Production environments. */
public struct PartnerEndCustomerSecrets: Codable, JSONEncodable, Hashable {

    /** The end customer's secret key for the Sandbox environment. */
    public var sandbox: String?
    /** The end customer's secret key for the Development environment. */
    public var development: String?

    public init(sandbox: String? = nil, development: String? = nil) {
        self.sandbox = sandbox
        self.development = development
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case sandbox
        case development
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(sandbox, forKey: .sandbox)
        try container.encodeIfPresent(development, forKey: .development)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        sandbox = try container.decodeIfPresent(String.self, forKey: .sandbox)
        development = try container.decodeIfPresent(String.self, forKey: .development)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("sandbox")
        nonAdditionalPropertyKeys.insert("development")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

