//
// IncomeVerificationPaystubsGetResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** IncomeVerificationPaystubsGetResponse defines the response schema for &#x60;/income/verification/paystubs/get&#x60;. */
public struct IncomeVerificationPaystubsGetResponse: Codable, JSONEncodable, Hashable {

    /** Metadata for an income document. */
    public var documentMetadata: [DocumentMetadata]?
    public var paystubs: [Paystub]
    public var error: PlaidError?
    /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
    public var requestId: String

    public init(documentMetadata: [DocumentMetadata]? = nil, paystubs: [Paystub], error: PlaidError? = nil, requestId: String) {
        self.documentMetadata = documentMetadata
        self.paystubs = paystubs
        self.error = error
        self.requestId = requestId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case documentMetadata = "document_metadata"
        case paystubs
        case error
        case requestId = "request_id"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(documentMetadata, forKey: .documentMetadata)
        try container.encode(paystubs, forKey: .paystubs)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encode(requestId, forKey: .requestId)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        documentMetadata = try container.decodeIfPresent([DocumentMetadata].self, forKey: .documentMetadata)
        paystubs = try container.decode([Paystub].self, forKey: .paystubs)
        error = try container.decodeIfPresent(PlaidError.self, forKey: .error)
        requestId = try container.decode(String.self, forKey: .requestId)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("document_metadata")
        nonAdditionalPropertyKeys.insert("paystubs")
        nonAdditionalPropertyKeys.insert("error")
        nonAdditionalPropertyKeys.insert("request_id")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

