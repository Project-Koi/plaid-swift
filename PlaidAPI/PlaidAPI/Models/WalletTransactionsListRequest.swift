//
// WalletTransactionsListRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** WalletTransactionListRequest defines the request schema for &#x60;/wallet/transaction/list&#x60; */
public struct WalletTransactionsListRequest: Codable, JSONEncodable, Hashable {

    static let walletIdRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    static let cursorRule = StringRule(minLength: nil, maxLength: 256, pattern: nil)
    static let countRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 200, exclusiveMaximum: false, multipleOf: nil)
    /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
    public var clientId: String?
    /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
    public var secret: String?
    /** The ID of the e-wallet to fetch transactions from */
    public var walletId: String
    /** A base64 value representing the latest transaction that has already been requested. Set this to `next_cursor` received from the previous `/wallet/transaction/list` request. If provided, the response will only contain transactions created before that transaction. If omitted, the response will contain transactions starting from the most recent, and in descending order by the `created_at` time. */
    public var cursor: String?
    /** The number of transactions to fetch */
    public var count: Int? = 10
    public var options: WalletTransactionListRequestOptions?

    public init(clientId: String? = nil, secret: String? = nil, walletId: String, cursor: String? = nil, count: Int? = 10, options: WalletTransactionListRequestOptions? = nil) {
        self.clientId = clientId
        self.secret = secret
        self.walletId = walletId
        self.cursor = cursor
        self.count = count
        self.options = options
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientId = "client_id"
        case secret
        case walletId = "wallet_id"
        case cursor
        case count
        case options
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientId, forKey: .clientId)
        try container.encodeIfPresent(secret, forKey: .secret)
        try container.encode(walletId, forKey: .walletId)
        try container.encodeIfPresent(cursor, forKey: .cursor)
        try container.encodeIfPresent(count, forKey: .count)
        try container.encodeIfPresent(options, forKey: .options)
    }
}

