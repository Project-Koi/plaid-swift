//
// TransferOriginatorCreateResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Defines the response schema for &#x60;/transfer/originator/create&#x60; */
public struct TransferOriginatorCreateResponse: Codable, JSONEncodable, Hashable {

    /** Client ID of the originator. This identifier will be used when creating transfers and should be stored associated with end user information. */
    public var originatorClientId: String
    /** The company name of the end customer. */
    public var companyName: String
    /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
    public var requestId: String

    public init(originatorClientId: String, companyName: String, requestId: String) {
        self.originatorClientId = originatorClientId
        self.companyName = companyName
        self.requestId = requestId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case originatorClientId = "originator_client_id"
        case companyName = "company_name"
        case requestId = "request_id"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(originatorClientId, forKey: .originatorClientId)
        try container.encode(companyName, forKey: .companyName)
        try container.encode(requestId, forKey: .requestId)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        originatorClientId = try container.decode(String.self, forKey: .originatorClientId)
        companyName = try container.decode(String.self, forKey: .companyName)
        requestId = try container.decode(String.self, forKey: .requestId)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("originator_client_id")
        nonAdditionalPropertyKeys.insert("company_name")
        nonAdditionalPropertyKeys.insert("request_id")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

