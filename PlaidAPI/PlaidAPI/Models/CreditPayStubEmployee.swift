//
// CreditPayStubEmployee.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Data about the employee. */
public struct CreditPayStubEmployee: Codable, JSONEncodable, Hashable {

    public var address: CreditPayStubAddress
    /** The name of the employee. */
    public var name: String?
    /** Marital status of the employee - either `SINGLE` or `MARRIED` or `NOT LISTED`. */
    public var maritalStatus: String?
    public var taxpayerId: PayStubTaxpayerID

    public init(address: CreditPayStubAddress, name: String?, maritalStatus: String?, taxpayerId: PayStubTaxpayerID) {
        self.address = address
        self.name = name
        self.maritalStatus = maritalStatus
        self.taxpayerId = taxpayerId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case address
        case name
        case maritalStatus = "marital_status"
        case taxpayerId = "taxpayer_id"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(address, forKey: .address)
        try container.encode(name, forKey: .name)
        try container.encode(maritalStatus, forKey: .maritalStatus)
        try container.encode(taxpayerId, forKey: .taxpayerId)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        address = try container.decode(CreditPayStubAddress.self, forKey: .address)
        name = try container.decodeIfPresent(String.self, forKey: .name)
        maritalStatus = try container.decodeIfPresent(String.self, forKey: .maritalStatus)
        taxpayerId = try container.decode(PayStubTaxpayerID.self, forKey: .taxpayerId)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("address")
        nonAdditionalPropertyKeys.insert("name")
        nonAdditionalPropertyKeys.insert("marital_status")
        nonAdditionalPropertyKeys.insert("taxpayer_id")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

