//
// SandboxPublicTokenCreateRequestOptionsTransactions.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** An optional set of parameters corresponding to transactions options. */
public struct SandboxPublicTokenCreateRequestOptionsTransactions: Codable, JSONEncodable, Hashable {

    static let daysRequestedRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 730, exclusiveMaximum: false, multipleOf: nil)
    /** The earliest date for which to fetch transaction history. Dates should be formatted as YYYY-MM-DD. */
    public var startDate: Date?
    /** The most recent date for which to fetch transaction history. Dates should be formatted as YYYY-MM-DD. */
    public var endDate: Date?
    /** The maximum number of days of transaction history to request for the Transactions product. */
    public var daysRequested: Int? = 90

    public init(startDate: Date? = nil, endDate: Date? = nil, daysRequested: Int? = 90) {
        self.startDate = startDate
        self.endDate = endDate
        self.daysRequested = daysRequested
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case startDate = "start_date"
        case endDate = "end_date"
        case daysRequested = "days_requested"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(startDate, forKey: .startDate)
        try container.encodeIfPresent(endDate, forKey: .endDate)
        try container.encodeIfPresent(daysRequested, forKey: .daysRequested)
    }
}

