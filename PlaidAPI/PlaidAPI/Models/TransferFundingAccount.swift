//
// TransferFundingAccount.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The originator&#39;s funding account, linked with Plaid Link or &#x60;/transfer/migrate_account&#x60;. */
public struct TransferFundingAccount: Codable, JSONEncodable, Hashable {

    /** The access token associated with the Item data is being requested for. */
    public var accessToken: String
    /** The Plaid `account_id` for the newly created Item. */
    public var accountId: String

    public init(accessToken: String, accountId: String) {
        self.accessToken = accessToken
        self.accountId = accountId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accessToken = "access_token"
        case accountId = "account_id"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(accessToken, forKey: .accessToken)
        try container.encode(accountId, forKey: .accountId)
    }
}

