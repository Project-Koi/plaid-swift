//
// AccountAssets.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Asset information about an account */
public struct AccountAssets: Codable, JSONEncodable, Hashable {

    public enum VerificationStatus: String, Codable, CaseIterable {
        case automaticallyVerified = "automatically_verified"
        case pendingAutomaticVerification = "pending_automatic_verification"
        case pendingManualVerification = "pending_manual_verification"
        case manuallyVerified = "manually_verified"
        case verificationExpired = "verification_expired"
        case verificationFailed = "verification_failed"
        case databaseMatched = "database_matched"
    }
    /** Plaid’s unique identifier for the account. This value will not change unless Plaid can't reconcile the account with the data returned by the financial institution. This may occur, for example, when the name of the account changes. If this happens a new `account_id` will be assigned to the account.  The `account_id` can also change if the `access_token` is deleted and the same credentials that were used to generate that `access_token` are used to generate a new `access_token` on a later date. In that case, the new `account_id` will be different from the old `account_id`.  If an account with a specific `account_id` disappears instead of changing, the account is likely closed. Closed accounts are not returned by the Plaid API.  Like all Plaid identifiers, the `account_id` is case sensitive. */
    public var accountId: String
    public var balances: AssetReportAccountBalance
    /** The last 2-4 alphanumeric characters of an account's official account number. Note that the mask may be non-unique between an Item's accounts, and it may also not match the mask that the bank displays to the user. */
    public var mask: String?
    /** The name of the account, either assigned by the user or by the financial institution itself */
    public var name: String
    /** The official name of the account as given by the financial institution */
    public var officialName: String?
    public var type: AccountType
    public var subtype: AccountSubtype?
    /** The current verification status of an Auth Item initiated through Automated or Manual micro-deposits.  Returned for Auth Items only.  `pending_automatic_verification`: The Item is pending automatic verification  `pending_manual_verification`: The Item is pending manual micro-deposit verification. Items remain in this state until the user successfully verifies the micro-deposit.  `automatically_verified`: The Item has successfully been automatically verified   `manually_verified`: The Item has successfully been manually verified  `verification_expired`: Plaid was unable to automatically verify the deposit within 7 calendar days and will no longer attempt to validate the Item. Users may retry by submitting their information again through Link.  `verification_failed`: The Item failed manual micro-deposit verification because the user exhausted all 3 verification attempts. Users may retry by submitting their information again through Link.  `database_matched`: The Item has successfully been verified using Plaid's data sources. Note: Database Match is currently a beta feature, please contact your account manager for more information.    */
    public var verificationStatus: VerificationStatus?
    /** A unique and persistent identifier for accounts that can be used to trace multiple instances of the same account across different Items for depository accounts. This is currently an opt-in field and only supported for Chase Items. */
    public var persistentAccountId: String?
    /** The duration of transaction history available for this Item, typically defined as the time since the date of the earliest transaction in that account. */
    public var daysAvailable: Double
    /** Transaction history associated with the account. */
    public var transactions: [AssetReportTransaction]
    public var investments: AssetReportInvestments?
    /** Data returned by the financial institution about the account owner or owners.For business accounts, the name reported may be either the name of the individual or the name of the business, depending on the institution. Multiple owners on a single account will be represented in the same `owner` object, not in multiple owner objects within the array. In API versions 2018-05-22 and earlier, the `owners` object is not returned, and instead identity information is returned in the top level `identity` object. For more details, see [Plaid API versioning](https://plaid.com/docs/api/versioning/#version-2019-05-29) */
    public var owners: [Owner]
    public var ownershipType: OwnershipType?
    /** Calculated data about the historical balances on the account. */
    public var historicalBalances: [HistoricalBalance]

    public init(accountId: String, balances: AssetReportAccountBalance, mask: String?, name: String, officialName: String?, type: AccountType, subtype: AccountSubtype?, verificationStatus: VerificationStatus? = nil, persistentAccountId: String? = nil, daysAvailable: Double, transactions: [AssetReportTransaction], investments: AssetReportInvestments? = nil, owners: [Owner], ownershipType: OwnershipType? = nil, historicalBalances: [HistoricalBalance]) {
        self.accountId = accountId
        self.balances = balances
        self.mask = mask
        self.name = name
        self.officialName = officialName
        self.type = type
        self.subtype = subtype
        self.verificationStatus = verificationStatus
        self.persistentAccountId = persistentAccountId
        self.daysAvailable = daysAvailable
        self.transactions = transactions
        self.investments = investments
        self.owners = owners
        self.ownershipType = ownershipType
        self.historicalBalances = historicalBalances
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accountId = "account_id"
        case balances
        case mask
        case name
        case officialName = "official_name"
        case type
        case subtype
        case verificationStatus = "verification_status"
        case persistentAccountId = "persistent_account_id"
        case daysAvailable = "days_available"
        case transactions
        case investments
        case owners
        case ownershipType = "ownership_type"
        case historicalBalances = "historical_balances"
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(accountId, forKey: .accountId)
        try container.encode(balances, forKey: .balances)
        try container.encode(mask, forKey: .mask)
        try container.encode(name, forKey: .name)
        try container.encode(officialName, forKey: .officialName)
        try container.encode(type, forKey: .type)
        try container.encode(subtype, forKey: .subtype)
        try container.encodeIfPresent(verificationStatus, forKey: .verificationStatus)
        try container.encodeIfPresent(persistentAccountId, forKey: .persistentAccountId)
        try container.encode(daysAvailable, forKey: .daysAvailable)
        try container.encode(transactions, forKey: .transactions)
        try container.encodeIfPresent(investments, forKey: .investments)
        try container.encode(owners, forKey: .owners)
        try container.encodeIfPresent(ownershipType, forKey: .ownershipType)
        try container.encode(historicalBalances, forKey: .historicalBalances)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        accountId = try container.decode(String.self, forKey: .accountId)
        balances = try container.decode(AssetReportAccountBalance.self, forKey: .balances)
        mask = try container.decodeIfPresent(String.self, forKey: .mask)
        name = try container.decode(String.self, forKey: .name)
        officialName = try container.decodeIfPresent(String.self, forKey: .officialName)
        type = try container.decode(AccountType.self, forKey: .type)
        subtype = try container.decodeIfPresent(AccountSubtype.self, forKey: .subtype)
        verificationStatus = try container.decodeIfPresent(VerificationStatus.self, forKey: .verificationStatus)
        persistentAccountId = try container.decodeIfPresent(String.self, forKey: .persistentAccountId)
        daysAvailable = try container.decode(Double.self, forKey: .daysAvailable)
        transactions = try container.decode([AssetReportTransaction].self, forKey: .transactions)
        investments = try container.decodeIfPresent(AssetReportInvestments.self, forKey: .investments)
        owners = try container.decode([Owner].self, forKey: .owners)
        ownershipType = try container.decodeIfPresent(OwnershipType.self, forKey: .ownershipType)
        historicalBalances = try container.decode([HistoricalBalance].self, forKey: .historicalBalances)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("account_id")
        nonAdditionalPropertyKeys.insert("balances")
        nonAdditionalPropertyKeys.insert("mask")
        nonAdditionalPropertyKeys.insert("name")
        nonAdditionalPropertyKeys.insert("official_name")
        nonAdditionalPropertyKeys.insert("type")
        nonAdditionalPropertyKeys.insert("subtype")
        nonAdditionalPropertyKeys.insert("verification_status")
        nonAdditionalPropertyKeys.insert("persistent_account_id")
        nonAdditionalPropertyKeys.insert("days_available")
        nonAdditionalPropertyKeys.insert("transactions")
        nonAdditionalPropertyKeys.insert("investments")
        nonAdditionalPropertyKeys.insert("owners")
        nonAdditionalPropertyKeys.insert("ownership_type")
        nonAdditionalPropertyKeys.insert("historical_balances")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

