//
// KYCCheckAddressSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Result summary object specifying how the &#x60;address&#x60; field matched. */
public struct KYCCheckAddressSummary: Codable, JSONEncodable, Hashable {

    public var summary: MatchSummaryCode
    public var poBox: POBoxStatus
    public var type: AddressPurposeLabel

    public init(summary: MatchSummaryCode, poBox: POBoxStatus, type: AddressPurposeLabel) {
        self.summary = summary
        self.poBox = poBox
        self.type = type
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case summary
        case poBox = "po_box"
        case type
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(summary, forKey: .summary)
        try container.encode(poBox, forKey: .poBox)
        try container.encode(type, forKey: .type)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        summary = try container.decode(MatchSummaryCode.self, forKey: .summary)
        poBox = try container.decode(POBoxStatus.self, forKey: .poBox)
        type = try container.decode(AddressPurposeLabel.self, forKey: .type)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("summary")
        nonAdditionalPropertyKeys.insert("po_box")
        nonAdditionalPropertyKeys.insert("type")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

